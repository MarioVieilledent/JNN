package src.main;

public class Main {
    public static void main(String[] args) {
        Layer inputLayer = new Layer(LayerType.Input);
        Layer hiddenLayer = new Layer(LayerType.Hidden);
        Layer outputLayer = new Layer(LayerType.Output);
    }
}
